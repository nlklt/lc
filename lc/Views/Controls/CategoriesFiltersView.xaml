<UserControl x:Class="lc.Views.Controls.CategoriesFiltersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:lc.Views.Controls" 
             xmlns:viewmodels="clr-namespace:lc.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:CatalogViewModel}">

    <Border Background="{DynamicResource SurfaceBrush}"
            CornerRadius="8"
            Padding="12">

        <Border.RenderTransform>
            <TranslateTransform X="100"/>
        </Border.RenderTransform>

        <Border.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="0" To="1"
                                     Duration="0:0:0.3" />
                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TranslateTransform.X)"
                                     From="40" To="0"
                                     Duration="0:0:0.3" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>
        
        <StackPanel>
            <Border Padding="5,4" 
                    Margin="0,0,0,4" 
                    CornerRadius="5">
            
                <Border.InputBindings>
                    <MouseBinding Gesture="LeftClick"
                                  Command="{Binding ToggleCategoriesCommand}"/>
                </Border.InputBindings>
            
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                        
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource SecondaryHoverBrush}"/>
                            </Trigger>
                        
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <Grid>
                    <TextBlock Text="Назад" FontSize="16"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               HorizontalAlignment="Left"/>
                
                    <TextBlock Text="{Binding CategoriesSummaryText}"  FontSize="16"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               HorizontalAlignment="Right"/>
                </Grid>
            </Border>

            <CheckBox Content="Строгое соответствие"
                      Style="{DynamicResource CheckBoxTwoStateStyle}"
                      Foreground="{DynamicResource TextPrimaryBrush}"
                      IsChecked="{Binding IsStrictCategoryMatch, Mode=TwoWay}"
                      Margin="0,4"/>

            <ItemsControl ItemsSource="{Binding CategoryOptions}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Name}"
                                  Style="{DynamicResource CheckBoxThreeStateStyle}"
                                  Foreground="{DynamicResource TextPrimaryBrush}"
                                  IsThreeState="True"
                                  IsChecked="{Binding State, Mode=TwoWay,
                                  Converter={StaticResource CheckBoxStateToNullableBoolConverter}}"
                                  Margin="0,2"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Border>
</UserControl>
