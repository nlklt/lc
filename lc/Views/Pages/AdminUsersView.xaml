<UserControl x:Class="lc.Views.Pages.AdminUsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:lc.ViewModels" 
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:AdminUsersViewModel}" 
             d:DesignHeight="442.58" d:DesignWidth="919.31">

    <UserControl.Resources>
        <Style x:Key="InfoCardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Padding" Value="12"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SecondaryBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource TextInverseBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Border Style="{DynamicResource InfoCardStyle}" Margin="4">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0*"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <DockPanel Margin="0,0,0,12" Grid.ColumnSpan="2">
                <TextBlock Text="Пользователи"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           FontSize="16"
                           FontWeight="SemiBold"
                           DockPanel.Dock="Left" 
                           VerticalAlignment="Center"/>

                <Button Content="Обновить"
                        Command="{Binding ReloadCommand}"
                        Style="{DynamicResource SecondaryButtonStyle}"
                        FontSize="12"
                        HorizontalAlignment="Right" />
            </DockPanel>

            <DataGrid Grid.Row="2" Grid.ColumnSpan="2"
                      ItemsSource="{Binding Users}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="{DynamicResource BorderBrush}"
                      VerticalGridLinesBrush="{DynamicResource BorderBrush}"
                      RowHeight="36"
                      BorderBrush="{DynamicResource TransparentBrush}"
                      Background="{DynamicResource SurfaceBrush}"
                      RowBackground="{DynamicResource SurfaceBrush}"
                      AlternatingRowBackground="{DynamicResource SurfaceAltBrush}"
                      Foreground="{DynamicResource TextPrimaryBrush}"
                      HorizontalAlignment="Center"
                      SelectionMode="Single">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID"             Binding="{Binding UserId}" Width="4*" />
                    <DataGridTextColumn Header="Логин"          Binding="{Binding UserName}" Width="16*" />
                    <DataGridTextColumn Header="Роль"           Binding="{Binding RoleText}" Width="6*" />
                    <DataGridTextColumn Header="Создан"         Binding="{Binding CreatedAt, StringFormat={}{0:dd.MM.yyyy}}" Width="10*" />
                    <DataGridTextColumn Header="Комментарии"    Binding="{Binding CommentsCount}" Width="10*" />
                    <DataGridTextColumn Header="Книги"          Binding="{Binding PublishedBooksCount}" Width="6*" />
                    <DataGridTextColumn Header="Блокировка комментариев" Binding="{Binding BlockedCommentsText}" Width="18*" />
                    <DataGridTemplateColumn Header="Действия" Width="16*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Content="Удалить"
                                            Command="{Binding DataContext.DeleteUserCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{DynamicResource SecondaryButtonStyle}"
                                            FontSize="12" Margin="0,0,4,0"
                                            Foreground="{DynamicResource WarningFrontBrush}"
                                            BorderBrush="{DynamicResource WarningFrontBrush}"
                                            Background="{DynamicResource WarningBackBrush}"
                                            Visibility="{Binding CanDelete, Converter={StaticResource BoolToVisibilityConverter}}" />

                                    <Button Content="{Binding CommentBlockText}"
                                            Command="{Binding DataContext.ToggleCommentBlockCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{DynamicResource SecondaryButtonStyle}"
                                            FontSize="12"
                                            Margin="0,0,6,0"
                                            Visibility="{Binding CanToggleCommentBlock, Converter={StaticResource BoolToVisibilityConverter}}" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</UserControl>