<UserControl x:Class = "lc.Views.Pages.AdminUsersView"
             xmlns = "http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x = "http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc = "http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d = "http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm = "clr-namespace:lc.ViewModels" 
             mc:Ignorable = "d" 
             d:DataContext="{d:DesignInstance Type=vm:AdminUsersViewModel}">

    <UserControl.Resources>
        <Style x:Key="InfoCardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </UserControl.Resources>

    <Border Style = "{DynamicResource InfoCardStyle}" >
        <Grid >
            <Grid.RowDefinitions>
                <RowDefinition Height = "Auto" />
                <RowDefinition Height = "Auto" />
                <RowDefinition Height = "*" />
            </Grid.RowDefinitions>

            <DockPanel Margin = "0,0,0,12" >
                <TextBlock Text = "Пользователи"
                           Foreground = "{DynamicResource TextPrimaryBrush}"
                           FontSize = "18"
                           FontWeight = "SemiBold"
                           DockPanel.Dock = "Left" />

                <Button Content = "Обновить"
                        Command = "{Binding ReloadCommand}"
                        Style = "{DynamicResource SecondaryButtonStyle}"
                        HorizontalAlignment = "Right" />
            </DockPanel >

            <DataGrid Grid.Row = "2"
                      ItemsSource = "{Binding Users}"
                      AutoGenerateColumns = "False"
                      IsReadOnly = "True"
                      CanUserAddRows = "False"
                      CanUserDeleteRows = "False"
                      HeadersVisibility = "Column"
                      GridLinesVisibility = "Horizontal"
                      RowHeight = "36"
                      SelectionMode = "Single" >

                <DataGrid.Columns>
                    <DataGridTextColumn Header = "ID" Binding = "{Binding UserId}" Width = "70" />
                    <DataGridTextColumn Header = "Логин" Binding = "{Binding UserName}" Width = "*" />
                    <DataGridTextColumn Header = "Роль" Binding = "{Binding RoleText}" Width = "110" />
                    <DataGridTextColumn Header = "Создан" Binding = "{Binding CreatedAt, StringFormat={}{0:dd.MM.yyyy}}" Width = "110" />
                    <DataGridTextColumn Header = "Комменты" Binding = "{Binding CommentsCount}" Width = "90" />
                    <DataGridTextColumn Header = "Книги" Binding = "{Binding PublishedBooksCount}" Width = "80" />
                    <DataGridTextColumn Header = "Запрет" Binding = "{Binding BlockedCommentsText}" Width = "80" />

                    <DataGridTemplateColumn Header = "Действия" Width = "240" >
                        <DataGridTemplateColumn.CellTemplate >
                            <DataTemplate >
                                <StackPanel Orientation = "Horizontal" HorizontalAlignment = "Right" >
                                    <Button Content = "{Binding CommentBlockText}"
                                            Command = "{Binding DataContext.ToggleCommentBlockCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter = "{Binding}"
                                            Style = "{DynamicResource SecondaryButtonStyle}"
                                            FontSize = "12"
                                            Margin = "0,0,6,0"
                                            Visibility = "{Binding CanToggleCommentBlock, Converter={StaticResource BoolToVisibilityConverter}}" />

                                    <Button Content = "Удалить"
                                            Command = "{Binding DataContext.DeleteUserCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter = "{Binding}"
                                            Style = "{DynamicResource SecondaryButtonStyle}"
                                            FontSize = "12"
                                            Foreground = "{DynamicResource WarningFrontBrush}"
                                            BorderBrush = "{DynamicResource WarningFrontBrush}"
                                            Background = "{DynamicResource WarningBackBrush}"
                                            Visibility = "{Binding CanDelete, Converter={StaticResource BoolToVisibilityConverter}}" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</UserControl>