<UserControl x:Class="lc.Views.Pages.CatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:lc.ViewModels" 
             xmlns:vc="clr-namespace:lc.Views.Controls"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:CatalogViewModel}"
             d:DesignHeight="700"
             d:DesignWidth="1200">

    <Grid Background="{StaticResource BackgroundBrush}" Margin="4">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="275"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <Border Grid.Row="0" Grid.ColumnSpan="2" Panel.ZIndex="999"
                Background="{StaticResource SurfaceBrush}"
                CornerRadius="8" Margin="0,0,0,4" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Style="{StaticResource TextBoxPlaceholderStyle}"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Tag="Названию, автор" />

                <ComboBox Grid.Column="1" Style="{StaticResource ComboBoxBaseStyle}"
                          ItemsSource="{Binding SortFields}"
                          SelectedItem="{Binding SelectedSortField, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          VerticalContentAlignment="Center" Margin="12,0"/>

                <Border Grid.Column="2" Background="{StaticResource SurfaceBrush}">
                    <CheckBox 
                          Content="По возрастанию" Style="{StaticResource CheckBoxTwoStateStyle}"
                          IsChecked="{Binding SortAscending, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          VerticalAlignment="Center"
                          Foreground="{DynamicResource TextPrimaryBrush}" />
                </Border>
            </Grid>
        </Border>

        <!-- Каталог -->
        <Border Grid.Row="1"
                Grid.Column="0"
                Background="Transparent"
                Margin="0,0,0,4">

            <ListBox ItemsSource="{Binding Books}"
                     SelectedItem="{Binding SelectedBook, Mode=TwoWay}"
                     Background="Transparent"
                     BorderThickness="0"  Margin="-6,-12"
                     HorizontalContentAlignment="Stretch"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Hidden">

                <!-- Используем WrapPanel для адаптивной сетки -->
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="True"
                                   HorizontalAlignment="Center" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="6,12"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="Transparent"
                                Width="166" 
                                Cursor="Hand">

                            <!-- Переход в детали ЛКМ -->
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftClick" 
                                      Command="{Binding DataContext.OpenDetailsCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                      CommandParameter="{Binding}"/>
                            </Border.InputBindings>

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="245"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Обложка -->
                                <Path Grid.Row="0">
                                    <Path.Data>
                                        <RectangleGeometry Rect="0,0,160,236" 
                                                           RadiusX="6" 
                                                           RadiusY="6" />
                                    </Path.Data>
                                    <Path.Fill>
                                        <ImageBrush ImageSource="{Binding CoverImagePath}" 
                                                    Stretch="UniformToFill" 
                                                    RenderOptions.BitmapScalingMode="HighQuality"/>
                                    </Path.Fill>
                                </Path>

                                <!-- Рейтинг 
                                <Border HorizontalAlignment="Left" 
                                VerticalAlignment="Top" 
                                Background="#4CAF50" 
                                CornerRadius="4" 
                                Margin="6,6,0,0"
                                Padding="4,2">
                                    <TextBlock Text="{Binding Rating, StringFormat={}{0:F1}}" 
                                       Foreground="White" 
                                       FontWeight="Bold" 
                                       FontSize="12"/>
                                </Border> -->

                                <!-- Инфо -->
                                <StackPanel Grid.Row="1" Margin="0,0,0,0">
                                    <TextBlock Text="{Binding Title}"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       TextWrapping="Wrap"
                                       MaxHeight="40"
                                       TextTrimming="CharacterEllipsis"/>

                                    <TextBlock Text="{Binding AuthorName}"
                                       FontSize="12"
                                       Foreground="{DynamicResource TextSecondaryBrush}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Фильтры -->
        <vc:FiltersView Grid.Row="1"
                        Grid.Column="1"/>
    </Grid>

</UserControl>